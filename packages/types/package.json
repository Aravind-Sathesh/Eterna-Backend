{
  "name": "@eterna/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Eterna services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "echo \"No tests for types package\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
